<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.spc.dao.GradePercentDao">

    <update id="insertGradePercent">
        update course.gradePercent set course.gradePercent.gradePercentage=#{knsk},course.gradePercent.operator=#{userId},course.gradePercent.operateTime=#{date}
        where course.gradePercent.id=2;
        update course.gradePercent set course.gradePercent.gradePercentage=#{xbsj},course.gradePercent.operator=#{userId},course.gradePercent.operateTime=#{date}
        where course.gradePercent.id=1;
        update course.gradePercent set course.gradePercent.gradePercentage=#{zzxx},course.gradePercent.operator=#{userId},course.gradePercent.operateTime=#{date}
        where course.gradePercent.id=3;
        update course.gradePercent set course.gradePercent.gradePercentage=#{dekt},course.gradePercent.operator=#{userId},course.gradePercent.operateTime=#{date}
        where course.gradePercent.id=4;
        update course.gradePercent set course.gradePercent.gradePercentage=#{qmnl},course.gradePercent.operator=#{userId},course.gradePercent.operateTime=#{date}
        where course.gradePercent.id=5;
    </update>

    <update id="updateFlagZero">
       update course.gradePercent set flag=0
    </update>

    <select id="getGradePercent" resultType="map" >
        SELECT * FROM course.gradePercent
    </select>
</mapper>
